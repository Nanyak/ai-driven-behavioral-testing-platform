# Elasticsearch source for ingestion. Defaults match the root .env / infra stack.
ELASTICSEARCH_URL=http://localhost:9200
ELASTICSEARCH_INDEX=behavior-logs-*

# Default ingestion window when --from / --to are not passed (hours back from now).
INGEST_WINDOW_HOURS=24
