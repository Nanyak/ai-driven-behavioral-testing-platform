{
  "name": "@platform/behavior-engine",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Behavioral modeling engine: mines unlabeled session flows into ranked, persona-tagged test candidates (emergent classification, deterministic; LLM for naming only).",
  "scripts": {
    "mine": "node --max-old-space-size=2048 --import tsx src/run.ts",
    "test:signature": "tsx src/signature/signature.test.ts",
    "test:coverage": "tsx src/selection/coverage.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
